# Web server
flask
flask-cors

# Diffusion / ML stack
# NOTE: torch must be installed from the CUDA 12.8 wheel index for Blackwell
# GPUs (e.g. RTX 50-series / sm_120):
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu128
torch
diffusers
transformers
accelerate
safetensors
peft

# Image / data handling
Pillow
numpy
pandas
